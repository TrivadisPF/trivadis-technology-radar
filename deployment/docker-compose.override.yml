version: "3.5"

services:
  techradar:
    image: trivadis/trivadis-technology-radar:latest
    container_name: techradar
    hostname: techradar
    labels:
      com.centurylinklabs.watchtower.enable: 'true'
    ports:
      - 28540:8080
    
